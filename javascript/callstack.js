//1.Global Eecution context is different for different environment
     //for browser  value of this  object is window
      // js code is run in global execution context
    //1*.Memory phase(creation phase)   
       //memory allocation to all variables
        //function definition
    //2*.Execution phase
       //Variables get real values.
       // Functions execute when called 
       // eg When sayHello() runs, a new execution context(Memory phase+execution phase) is created inside the global one.
//2.function execution context
//3.Eval excution context

